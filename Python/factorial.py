# factorial by method
def facto(a,y):
    if(a==1):
        return print("the factorial of",a1,"! is >>>",y)
    else:
        y=y*a
        return facto(a-1,y)
y=1
a=a1=abs(int(input("Enter the number = ")))
facto(a,y)